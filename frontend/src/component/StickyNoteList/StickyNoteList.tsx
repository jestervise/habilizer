import {FunctionComponent} from 'react';

interface Props {
    count: number;
  }

const StickyNoteList:FunctionComponent<Props> = ({count = 0}) => {
    
    return (  );
}
 
export default StickyNoteList;